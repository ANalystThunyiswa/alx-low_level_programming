#include <stdio.h>

/**
 * main - A program that prints all single digit number.
 * of base 10 starting from 0.
 *
 * Return: 0 (Success)
 */

int main(void)
{
	int num;

	for( num = 0; num < 10; num++)
	{
		printf("%d\n", num);
	
	}
	return (0);
}
